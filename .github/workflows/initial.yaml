name: Initial Test

on:
  push:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "Test test/"
          echo 'something else'
          exit 1
